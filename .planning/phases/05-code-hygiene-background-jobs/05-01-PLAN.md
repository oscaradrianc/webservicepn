---
phase: 05-code-hygiene-background-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Negocio.Business/Utilidades/IUtilidades.cs
  - Negocio.Business/Utilidades/General.cs
  - Negocio.Business/Utilidades/Utilidades.cs
  - Negocio.Business/Proveedor/Proveedor.cs
  - PortalNegocioWS/appsettings.json
autonomous: true
requirements:
  - HYG-01
  - HYG-02
  - HYG-10

must_haves:
  truths:
    - "Utilidades.cs static class is deleted — no file at Negocio.Business/Utilidades/Utilidades.cs"
    - "SendMail signature is removed from IUtilidades.cs"
    - "SendMail method body is removed from General.cs (UtilidadesBusiness)"
    - "Proveedor.cs no longer contains hardcoded integer literal 4 for tipo identificacion"
    - "appsettings.json contains Settings:TipoIdentificacionJuridica key with value 4"
    - "dotnet build succeeds — no callers broke from the deletions"
  artifacts:
    - path: "Negocio.Business/Utilidades/IUtilidades.cs"
      provides: "IUtilidades interface without SendMail"
    - path: "Negocio.Business/Utilidades/General.cs"
      provides: "UtilidadesBusiness without SendMail method"
    - path: "PortalNegocioWS/appsettings.json"
      provides: "TipoIdentificacionJuridica configuration key"
  key_links:
    - from: "Negocio.Business/Proveedor/Proveedor.cs"
      to: "appsettings.json"
      via: "_configuration.GetValue<int>(\"Settings:TipoIdentificacionJuridica\")"
      pattern: "TipoIdentificacionJuridica"
---

<objective>
Consolidate utility classes (HYG-01): delete the static Utilidades.cs class (confirmed no callers), remove SendMail from IUtilidades and UtilidadesBusiness (email moves to EmailQueueService in Plan 05-03), audit the SHA-512 distribution discrepancy (HYG-02), and move the hardcoded tipo identificacion = 4 in Proveedor.cs to appsettings.json (HYG-10).

Purpose: Establishes clean IUtilidades contract before Plan 05-04 wires email queue into NotificacionBusiness. All callers of IUtilidades must compile cleanly after SendMail is removed.

Output: Deleted Utilidades.cs, trimmed IUtilidades interface, trimmed General.cs, appsettings.json with TipoIdentificacionJuridica key.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-code-hygiene-background-jobs/05-CONTEXT.md
@.planning/phases/05-code-hygiene-background-jobs/05-RESEARCH.md
@.planning/phases/05-code-hygiene-background-jobs/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Utilidades.cs and remove SendMail from IUtilidades + General.cs</name>
  <files>
    Negocio.Business/Utilidades/Utilidades.cs,
    Negocio.Business/Utilidades/IUtilidades.cs,
    Negocio.Business/Utilidades/General.cs
  </files>
  <read_first>
    - Negocio.Business/Utilidades/Utilidades.cs — read full file to confirm it is the static class with no callers before deleting
    - Negocio.Business/Utilidades/IUtilidades.cs — read full file to locate exact SendMail signature on line 30
    - Negocio.Business/Utilidades/General.cs — read full file to locate SendMail method (lines ~337–386) and its SMTP imports
  </read_first>
  <action>
Step 1 — Verify no callers exist before deleting Utilidades.cs:
Run: grep -r "Utilidades\." --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/
Expected result: zero matches (confirmed in research). If matches found, stop and report.

Step 2 — Delete Utilidades.cs:
Delete the file Negocio.Business/Utilidades/Utilidades.cs. This file contains a static class Utilidades with duplicate implementations of GetStringEncriptado, GetSecuencia, DecodificarArchivo, ConvertirMensaje, ObtenerBlob, GetConstante, SendMail — all duplicates of UtilidadesBusiness methods. No callers reference it.

Step 3 — Remove SendMail signature from IUtilidades.cs:
In Negocio.Business/Utilidades/IUtilidades.cs, delete line 30:
    void SendMail(List<string> listaCorreos, string asunto, string mensaje, bool bcc = false);
All other method signatures stay unchanged.

Step 4 — Remove SendMail method from General.cs:
In Negocio.Business/Utilidades/General.cs, delete the entire SendMail method (approximately lines 337–386). The method signature is:
    public void SendMail(List<string> listaCorreos, string asunto, string mensaje, bool bcc = false)
Delete from the method signature line through the closing brace of the method body.
After deletion, check if the System.Net.Mail using directive (line 7) is still needed by any other method in the file. If no other MailMessage or SmtpClient references remain, remove the using System.Net.Mail; line as well.

Step 5 — Run build to confirm no callers broke:
Run: dotnet build PortalNegocioWS.sln
If build fails with "does not contain a definition for 'SendMail'" errors, the caller is in NotificacionBusiness.cs — that will be fixed in Plan 05-04. Accept build errors ONLY in NotificacionBusiness.cs (which calls _utilidades.SendMail). Any other file with SendMail call errors must be investigated and fixed now.

Note: NotificacionBusiness.cs WILL produce a compile error after this task (it calls _utilidades.SendMail). This is expected — Plan 05-04 replaces those calls with _emailQueue.Queue. Document the expected errors in the task output.
  </action>
  <verify>
    <automated>
      grep -r "Utilidades\." --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null | grep -v "IUtilidades\|UtilidadesBusiness\|#" || echo "PASS: no static Utilidades. callers"
      grep "SendMail" Negocio.Business/Utilidades/IUtilidades.cs || echo "PASS: SendMail removed from IUtilidades"
      grep "public void SendMail" Negocio.Business/Utilidades/General.cs || echo "PASS: SendMail method removed from General.cs"
      test ! -f Negocio.Business/Utilidades/Utilidades.cs && echo "PASS: Utilidades.cs deleted" || echo "FAIL: Utilidades.cs still exists"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f Negocio.Business/Utilidades/Utilidades.cs` exits 0
    - `grep "SendMail" Negocio.Business/Utilidades/IUtilidades.cs` returns no matches
    - `grep "public void SendMail" Negocio.Business/Utilidades/General.cs` returns no matches
    - `grep -r "Utilidades\." --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/` returns zero matches (excluding IUtilidades, UtilidadesBusiness class names)
    - Build errors, if any, are only in Negocio.Business/Notificacion/Notificacion.cs (expected — fixed in Plan 05-04)
  </acceptance_criteria>
  <done>Utilidades.cs deleted, SendMail removed from IUtilidades interface and UtilidadesBusiness implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Move hardcoded tipo identificacion to config (HYG-10) + document HYG-02 SHA-512 audit</name>
  <files>
    Negocio.Business/Proveedor/Proveedor.cs,
    PortalNegocioWS/appsettings.json,
    PortalNegocioWS/appsettings.Development.json
  </files>
  <read_first>
    - Negocio.Business/Proveedor/Proveedor.cs — read lines 1–30 to confirm IConfiguration injection in constructor, then lines 70–90 and 250–265 to locate the two hardcoded = 4 occurrences
    - PortalNegocioWS/appsettings.json — read full file to find the Settings section and determine where to add TipoIdentificacionJuridica
    - PortalNegocioWS/appsettings.Development.json — read to confirm whether it overrides Settings section
  </read_first>
  <action>
Step 1 — Move hardcoded tipo identificacion = 4 to configuration:

In PortalNegocioWS/appsettings.json, find the "Settings" JSON object and add the key:
    "TipoIdentificacionJuridica": 4
Add it as a sibling of other Settings values in the same JSON object.

In Negocio.Business/Proveedor/Proveedor.cs, locate the two lines (at approximately lines 76 and 257) that contain:
    tblProveedor.CLASTIPOIDENTIFICACION2 = personaNatural ? request.TipoDocumento : 4; //TODO

For both occurrences, replace with:
    int tipoIdentificacionJuridica = _configuration.GetValue<int>("Settings:TipoIdentificacionJuridica");
    tblProveedor.CLASTIPOIDENTIFICACION2 = personaNatural ? request.TipoDocumento : tipoIdentificacionJuridica;

Note: If _configuration is already injected in ProveedorBusiness constructor (line ~18), the variable declaration can be placed once at the top of the method body, not repeated for each occurrence. Inspect the method structure and place it appropriately (once per method if both occurrences are in different methods, once before the first use if they are in the same method).

Step 2 — Verify IConfiguration is already injected in ProveedorBusiness:
The constructor at Proveedor.cs line ~18 should have IConfiguration _configuration. If it does NOT exist, add it to the constructor and to the _configuration private readonly field declaration.

Step 3 — HYG-02 SHA-512 audit documentation:
Research confirms: UtilidadesBusiness.CreateSHA512 does double-hash (hash of hash) using SHA512.Create(). The deleted Utilidades.GetStringEncriptado did single hash using SHA512Managed. Research confirmed no callers of the static Utilidades class were found in main source. No code changes are needed for HYG-02 — the single-hash implementation was only in the now-deleted Utilidades.cs. Add a code comment to UtilidadesBusiness.CreateSHA512 method:
    // SHA-512 double-hash: hash(hash(input)). Standardized on this implementation (HYG-02).
    // The old Utilidades.GetStringEncriptado (single-hash, SHA512Managed) was deleted — had no callers.
  </action>
  <verify>
    <automated>
      grep "TipoIdentificacionJuridica" PortalNegocioWS/appsettings.json && echo "PASS: config key present"
      grep "= 4; //TODO" Negocio.Business/Proveedor/Proveedor.cs || echo "PASS: hardcoded = 4 removed"
      grep "TipoIdentificacionJuridica" Negocio.Business/Proveedor/Proveedor.cs && echo "PASS: config lookup present in Proveedor.cs"
      dotnet build PortalNegocioWS.sln 2>&1 | grep -E "^Build succeeded|error" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "TipoIdentificacionJuridica" PortalNegocioWS/appsettings.json` returns a match
    - `grep "= 4; //TODO" Negocio.Business/Proveedor/Proveedor.cs` returns no matches
    - `grep "_configuration.GetValue" Negocio.Business/Proveedor/Proveedor.cs` returns at least one match
    - `grep "TipoIdentificacionJuridica" Negocio.Business/Proveedor/Proveedor.cs` returns a match
    - `dotnet build PortalNegocioWS.sln` succeeds OR only fails on expected NotificacionBusiness.cs SendMail references
  </acceptance_criteria>
  <done>Hardcoded tipo identificacion moved to appsettings.json. SHA-512 is already standardized on double-hash CreateSHA512 — documented with comment. HYG-01, HYG-02, HYG-10 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| appsettings.json → ProveedorBusiness | Configuration value read at runtime — value 4 is an internal DB catalog ID, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | appsettings.json TipoIdentificacionJuridica | accept | Value is a static DB catalog reference, not a security boundary. Only operators with file system access can modify it. |
| T-05-01-02 | Information Disclosure | Deleted Utilidades.cs SHA-512 single-hash | mitigate | Single-hash implementation is removed. Double-hash CreateSHA512 is the surviving implementation. If any password hashes in POGEUSUARIO.USUA_CLAVE were created with the old single-hash path, they would already be in the DB and cannot be re-hashed without reset. The audit note documents this. |
</threat_model>

<verification>
After both tasks complete:
1. `test ! -f Negocio.Business/Utilidades/Utilidades.cs` — exits 0
2. `grep "SendMail" Negocio.Business/Utilidades/IUtilidades.cs` — no output
3. `grep "= 4; //TODO" Negocio.Business/Proveedor/Proveedor.cs` — no output
4. `grep "TipoIdentificacionJuridica" PortalNegocioWS/appsettings.json` — has output
5. `dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded"` — succeeds (only Notificacion.cs SendMail errors are acceptable at this stage)
</verification>

<success_criteria>
- Utilidades.cs static class deleted — confirmed by file non-existence
- IUtilidades no longer declares SendMail — grep confirms
- UtilidadesBusiness no longer implements SendMail — grep confirms
- Proveedor.cs uses _configuration.GetValue for tipo identificacion — grep confirms
- appsettings.json has Settings:TipoIdentificacionJuridica = 4 — grep confirms
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-hygiene-background-jobs/05-01-SUMMARY.md`
</output>
