---
phase: 05-code-hygiene-background-jobs
plan: 05
type: execute
wave: 3
depends_on:
  - "05-04"
files_modified:
  - Negocio.Business/Cotizacion/Cotizacion.cs
  - Negocio.Business/Solicitud/SolicitudCompra.cs
  - PortalNegocioWS/Controllers/UsuarioController.cs
autonomous: true
requirements:
  - BGD-02

must_haves:
  truths:
    - "No new Thread(...) exists in any .cs file in the main source (excluding worktrees)"
    - "Cotizacion.cs calls _notificacion.GenerarNotificacion directly — no Thread wrapper"
    - "SolicitudCompra.cs calls _notificacion.GenerarNotificacion directly — no Thread wrapper"
    - "UsuarioController.cs calls _notificacion.GenerarNotificacion directly — no Thread wrapper"
    - "dotnet build succeeds with zero errors"
  artifacts:
    - path: "Negocio.Business/Cotizacion/Cotizacion.cs"
      provides: "Cotizacion without Thread wrappers (3 removed)"
    - path: "Negocio.Business/Solicitud/SolicitudCompra.cs"
      provides: "SolicitudCompra without Thread wrappers (3 removed)"
    - path: "PortalNegocioWS/Controllers/UsuarioController.cs"
      provides: "UsuarioController without Thread wrappers (2 removed)"
  key_links:
    - from: "Negocio.Business/Cotizacion/Cotizacion.cs"
      to: "Negocio.Business/Notificacion/INotificacion.cs"
      via: "direct _notificacion.GenerarNotificacion call (no Thread)"
      pattern: "_notificacion.GenerarNotificacion"
---

<objective>
Remove the 8 remaining new Thread(...) fire-and-forget wrappers from Cotizacion.cs (3), SolicitudCompra.cs (3), and UsuarioController.cs (2). After Plan 05-04, NotificacionBusiness.GenerarNotificacion already queues email via IEmailQueue internally, so the Thread wrapper is no longer needed — callers can call GenerarNotificacion directly and synchronously.

Purpose: Complete BGD-02. After this plan, grep for "new Thread" in main source returns zero results.

Depends on: 05-04 (NotificacionBusiness now queues internally — Thread wrappers are the only thing left to remove).

Output: Updated Cotizacion.cs, SolicitudCompra.cs, UsuarioController.cs. Zero Thread wrappers remain.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-code-hygiene-background-jobs/05-PATTERNS.md
@.planning/phases/05-code-hygiene-background-jobs/05-04-SUMMARY.md
</context>

<interfaces>
<!-- Thread removal transformation pattern (from PATTERNS.md) -->

Before (Cotizacion.cs lines 93–103 representative):
```csharp
Thread t = new Thread(() =>
    _notificacion.GenerarNotificacion("registrocotizacion", request)
);
t.Start();
t.IsBackground = true;
```

After:
```csharp
_notificacion.GenerarNotificacion("registrocotizacion", request);
```

Before (UsuarioController.cs lines 143–149):
```csharp
Thread t = new Thread(() =>
{
    _notificacion.GenerarNotificacion("nuevousuario", usuario);
});
t.Start();
t.IsBackground = true;
```

After:
```csharp
_notificacion.GenerarNotificacion("nuevousuario", usuario);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove Thread wrappers from Cotizacion.cs and SolicitudCompra.cs (6 call sites)</name>
  <files>
    Negocio.Business/Cotizacion/Cotizacion.cs,
    Negocio.Business/Solicitud/SolicitudCompra.cs
  </files>
  <read_first>
    - Negocio.Business/Cotizacion/Cotizacion.cs — read lines 85–115 (first two Thread sites) and lines 400–415 (third Thread site) to see exact Thread construction and GenerarNotificacion arguments before removing
    - Negocio.Business/Solicitud/SolicitudCompra.cs — read lines 95–120 (two Thread sites) and lines 540–560 (third Thread site) for the same reason
  </read_first>
  <action>
Step 1 — Cotizacion.cs: Remove 3 Thread wrappers.

For each of the 3 Thread sites, the transformation is:
- Find the block: Thread tX = new Thread(() => ...) ; tX.Start(); tX.IsBackground = true;
- Replace the entire block with just the GenerarNotificacion call inside the lambda

Exact sites (verify line numbers by reading the file first):
- Lines ~93–99: Thread t + _notificacion.GenerarNotificacion("registrocotizacion", request)
- Lines ~99–105: Thread t1 + _notificacion.GenerarNotificacion("confirmacioncotizaci", request)
- Lines ~406–412: Thread t + _notificacion.GenerarNotificacion(some_referencia, some_obj)

After removing each block, the replacement is a single line:
    _notificacion.GenerarNotificacion("registrocotizacion", request);

After all 3 Thread blocks are removed from Cotizacion.cs:
- Check if System.Threading is still needed for other Thread/Monitor/Mutex usage: `grep "System.Threading\|Thread\|Monitor\|Mutex" Negocio.Business/Cotizacion/Cotizacion.cs`
- If no other Thread-related symbols remain, remove the `using System.Threading;` line.

Step 2 — SolicitudCompra.cs: Remove 3 Thread wrappers.

Same transformation: find each Thread block, replace with direct GenerarNotificacion call.

Exact sites (verify by reading):
- Lines ~101: Thread t + GenerarNotificacion call
- Lines ~111: Thread t1 + GenerarNotificacion call
- Lines ~548: Thread t + GenerarNotificacion call

After removal, check and optionally remove `using System.Threading;` if no other Thread usage.

Step 3 — Run partial build check after both files:
    dotnet build PortalNegocioWS.sln
Confirm no errors from the modified files.
  </action>
  <verify>
    <automated>
      grep "new Thread" Negocio.Business/Cotizacion/Cotizacion.cs || echo "PASS: no Thread in Cotizacion.cs"
      grep "new Thread" Negocio.Business/Solicitud/SolicitudCompra.cs || echo "PASS: no Thread in SolicitudCompra.cs"
      grep -c "_notificacion.GenerarNotificacion" Negocio.Business/Cotizacion/Cotizacion.cs | xargs echo "GenerarNotificacion calls in Cotizacion:"
      grep -c "_notificacion.GenerarNotificacion" Negocio.Business/Solicitud/SolicitudCompra.cs | xargs echo "GenerarNotificacion calls in SolicitudCompra:"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "new Thread" Negocio.Business/Cotizacion/Cotizacion.cs` returns no matches
    - `grep "new Thread" Negocio.Business/Solicitud/SolicitudCompra.cs` returns no matches
    - `grep "_notificacion.GenerarNotificacion" Negocio.Business/Cotizacion/Cotizacion.cs | wc -l` returns 3 (one direct call per removed Thread)
    - `grep "_notificacion.GenerarNotificacion" Negocio.Business/Solicitud/SolicitudCompra.cs | wc -l` returns 3
  </acceptance_criteria>
  <done>6 Thread wrappers removed from Cotizacion.cs and SolicitudCompra.cs. GenerarNotificacion called directly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Thread wrappers from UsuarioController.cs + verify zero Thread() in main source</name>
  <files>
    PortalNegocioWS/Controllers/UsuarioController.cs
  </files>
  <read_first>
    - PortalNegocioWS/Controllers/UsuarioController.cs — read lines 135–155 (first Thread site around line 143) and lines 232–248 (second Thread site around line 240) to see exact Thread construction before removing
  </read_first>
  <action>
Step 1 — UsuarioController.cs: Remove 2 Thread wrappers.

Exact sites (verify by reading the file):
- Lines ~143–149: Thread t + _notificacion.GenerarNotificacion("nuevousuario", usuario)
- Lines ~240–246: Thread t + _notificacion.GenerarNotificacion(some_referencia, some_obj)

For each, replace the entire Thread block (Thread declaration, Start(), IsBackground = true) with:
    _notificacion.GenerarNotificacion("nuevousuario", usuario);

Note: INotificacion is already injected in UsuarioController. No constructor change needed.

After removal, check if System.Threading is still needed:
    grep "Thread\|Monitor\|Mutex\|Interlocked" PortalNegocioWS/Controllers/UsuarioController.cs
If no other Thread-related usage, remove `using System.Threading;`.

Step 2 — Global verification: Confirm zero new Thread() in entire main source:
    grep -rn "new Thread(" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null
Expected: zero matches in main source (worktrees may still have them — exclude with path scope).

Step 3 — Run full build:
    dotnet build PortalNegocioWS.sln
Expected: Build succeeded, 0 errors.

Step 4 — Verify BGD-02 completion:
Run all three grep checks:
    grep -rn "new Thread" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null | grep -v "//\|worktree"
Expected: zero results.
  </action>
  <verify>
    <automated>
      grep "new Thread" PortalNegocioWS/Controllers/UsuarioController.cs || echo "PASS: no Thread in UsuarioController.cs"
      grep -rn "new Thread(" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null | grep -v "//" || echo "PASS: BGD-02 zero Thread sites in main source"
      dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded" && echo "PASS: build green"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "new Thread" PortalNegocioWS/Controllers/UsuarioController.cs` returns no matches
    - `grep -rn "new Thread(" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/` returns no matches (excluding comment lines)
    - `dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded"` present in output
    - `dotnet build PortalNegocioWS.sln 2>&1 | grep " error "` returns no matches
  </acceptance_criteria>
  <done>All 10 new Thread() fire-and-forget email wrappers removed. BGD-02 complete. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synchronous GenerarNotificacion call | Email is now queued synchronously from request thread to Channel — no new external boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Denial of Service | Synchronous GenerarNotificacion in request path | accept | GenerarNotificacion previously ran on a Thread; now it runs synchronously on the request thread. The email Queue() call inside it is ~1 microsecond (TryWrite on unbounded channel). No measurable latency impact. If GenerarNotificacion does heavy DB work before Queue(), it was already doing that on the spawned Thread — no regression. |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "new Thread(" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/` — no output
2. `dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded"` — present
3. `grep "_notificacion.GenerarNotificacion" Negocio.Business/Cotizacion/Cotizacion.cs | wc -l` — 3
4. `grep "_notificacion.GenerarNotificacion" Negocio.Business/Solicitud/SolicitudCompra.cs | wc -l` — 3
5. `grep "_notificacion.GenerarNotificacion" PortalNegocioWS/Controllers/UsuarioController.cs | wc -l` — 2
</verification>

<success_criteria>
- Zero new Thread() in Negocio.Business/, PortalNegocioWS/, SWNegocio/ — confirmed by grep
- dotnet build succeeds with zero errors
- All 10 Thread sites replaced by direct GenerarNotificacion calls
- BGD-02 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-hygiene-background-jobs/05-05-SUMMARY.md`
</output>
