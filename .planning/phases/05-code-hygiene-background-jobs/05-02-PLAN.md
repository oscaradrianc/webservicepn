---
phase: 05-code-hygiene-background-jobs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PortalNegocioWS/Program.cs
autonomous: true
requirements:
  - HYG-03
  - HYG-04
  - HYG-07

must_haves:
  truths:
    - "StartupCopia.cs does not exist in the repository (already confirmed deleted)"
    - "Program.cs has no commented-out code blocks — file is clean"
    - "No throw e pattern exists in any catch block in the codebase"
  artifacts:
    - path: "PortalNegocioWS/Program.cs"
      provides: "Clean Program.cs verified with no dead blocks"
  key_links: []
---

<objective>
Verify the three requirements that research confirmed are already satisfied (HYG-03, HYG-04, HYG-07) and produce a SUMMARY attesting to their status. These requirements do not require code changes — they are verification-only tasks based on the codebase reality check in RESEARCH.md.

Purpose: Close HYG-03, HYG-04, HYG-07 with documented verification so the executor log reflects complete requirement coverage. No code modifications expected.

Output: Grep outputs proving all three requirements are already satisfied; SUMMARY attesting closure.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-code-hygiene-background-jobs/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify HYG-03 (StartupCopia.cs absent), HYG-04 (no dead blocks in Program.cs), HYG-07 (no throw e pattern)</name>
  <files>
    PortalNegocioWS/Program.cs
  </files>
  <read_first>
    - PortalNegocioWS/Program.cs — read the full file to visually confirm there are no large commented-out blocks (research reported 128 lines, no commented blocks)
  </read_first>
  <action>
Step 1 — Verify HYG-03: StartupCopia.cs does not exist.
Run: find . -name "StartupCopia.cs" 2>/dev/null
Expected: zero results. Research confirms the file does not exist anywhere in the repo.
If the file IS found, read it, extract any Serilog configuration it contains to a note, then delete it.

Step 2 — Verify HYG-04: Program.cs has no commented-out dead code blocks.
Read PortalNegocioWS/Program.cs fully. Research reports 128 lines with no commented-out blocks. Confirm visually that there are no large commented sections (e.g., a block of 40+ lines commented with // or /* */). The requirement referenced a block at lines 97–138 that no longer exists.
If any commented blocks are found (more than single-line comments), delete them. If the file is already clean, no changes needed.

Step 3 — Verify HYG-07: No throw e pattern in catch blocks.
Run: grep -rn "throw [a-z]" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null
Expected: zero matches. Research confirms no throw e pattern found in main source. The pattern to find would be lines like:
    catch (Exception e) { throw e; }
    catch (Exception ex) { throw ex; }
If any matches ARE found, fix each: replace `throw e;` with `throw;` and `throw ex;` with `throw;`.
The bare throw keyword re-throws without resetting the stack trace; throw e creates a new stack trace from that point.

Step 4 — Document results.
If all three verifications pass with no changes needed, this is a pure verification task. Produce the SUMMARY reflecting that research was accurate and all three requirements were already satisfied at Phase 5 start.
  </action>
  <verify>
    <automated>
      find . -name "StartupCopia.cs" 2>/dev/null | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: HYG-03 StartupCopia.cs not present" || echo "FAIL: StartupCopia.cs found"'
      grep -rn "throw [a-z]" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/ 2>/dev/null | grep -v "//\|throwaway\|thrown" | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: HYG-07 no throw e pattern" || echo "FAIL: throw e patterns found"'
      wc -l PortalNegocioWS/Program.cs | awk '{print $1}' | xargs -I{} sh -c 'test {} -le 150 && echo "PASS: HYG-04 Program.cs line count reasonable" || echo "WARNING: Program.cs has {} lines — verify manually"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `find . -name "StartupCopia.cs"` returns no output (exit 0, no files found)
    - `grep -rn "throw [a-z]" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/` returns no matches
    - PortalNegocioWS/Program.cs contains no commented-out code blocks of more than 3 consecutive lines
    - If any of these checks fail, the corresponding fix is applied and re-verified
  </acceptance_criteria>
  <done>HYG-03, HYG-04, HYG-07 verified as already satisfied. No code changes needed (or fixes applied if checks failed). Requirements closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan makes no security-relevant changes. Verification only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | StartupCopia.cs (if found) | mitigate | If file exists and contains connection strings or secrets, delete it and confirm it is not tracked in git history with credentials |
</threat_model>

<verification>
1. `find . -name "StartupCopia.cs"` — no output
2. `grep -rn "throw [a-z]" --include="*.cs" Negocio.Business/ PortalNegocioWS/ SWNegocio/` — no output (excluding comment lines)
3. `wc -l PortalNegocioWS/Program.cs` — 150 lines or fewer, no large commented blocks visible in Read output
</verification>

<success_criteria>
- HYG-03 closed: StartupCopia.cs absent from repo — confirmed by find command
- HYG-04 closed: Program.cs has no dead code blocks — confirmed by file read
- HYG-07 closed: No throw e in catch blocks — confirmed by grep
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-hygiene-background-jobs/05-02-SUMMARY.md`
</output>
