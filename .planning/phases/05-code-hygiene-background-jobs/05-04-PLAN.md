---
phase: 05-code-hygiene-background-jobs
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-03"
files_modified:
  - Negocio.Business/Notificacion/Notificacion.cs
  - Negocio.Business/Preguntas/Preguntas.cs
autonomous: true
requirements:
  - BGD-02

must_haves:
  truths:
    - "NotificacionBusiness.cs calls _emailQueue.Queue(...) instead of _utilidades.SendMail(...)"
    - "IEmailQueue is injected into NotificacionBusiness constructor"
    - "Preguntas.cs constructor injects INotificacion instead of using new NotificacionBusiness(...)"
    - "dotnet build succeeds with zero errors after this plan"
  artifacts:
    - path: "Negocio.Business/Notificacion/Notificacion.cs"
      provides: "NotificacionBusiness using IEmailQueue for email dispatch"
    - path: "Negocio.Business/Preguntas/Preguntas.cs"
      provides: "PreguntasBusiness injecting INotificacion instead of new NotificacionBusiness"
  key_links:
    - from: "Negocio.Business/Notificacion/Notificacion.cs"
      to: "Negocio.Business/Email/IEmailQueue.cs"
      via: "constructor injection + _emailQueue.Queue(new EmailMessage(...))"
      pattern: "_emailQueue.Queue"
    - from: "Negocio.Business/Preguntas/Preguntas.cs"
      to: "Negocio.Business/Notificacion/INotificacion.cs"
      via: "constructor injection of INotificacion"
      pattern: "INotificacion"
---

<objective>
Wire the email queue into NotificacionBusiness: inject IEmailQueue, replace all _utilidades.SendMail(...) calls with _emailQueue.Queue(new EmailMessage(...)), and fix PreguntasBusiness to inject INotificacion instead of instantiating new NotificacionBusiness inline. After this plan, dotnet build must succeed with zero errors.

Purpose: NotificacionBusiness is the single email dispatch point. All 10 Thread call sites call _notificacion.GenerarNotificacion() which calls SendMail internally. After this plan, the email path is: GenerarNotificacion → _emailQueue.Queue → Channel → EmailQueueService → SmtpClient.

Depends on: 05-01 (SendMail removed from IUtilidades so Notificacion.cs has compile errors to fix), 05-03 (IEmailQueue exists to inject).

Output: Updated Notificacion.cs, updated Preguntas.cs. Build succeeds.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-code-hygiene-background-jobs/05-CONTEXT.md
@.planning/phases/05-code-hygiene-background-jobs/05-PATTERNS.md
@.planning/phases/05-code-hygiene-background-jobs/05-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. -->

IEmailQueue.cs (created in Plan 05-03):
```csharp
namespace Negocio.Business.Email
{
    public interface IEmailQueue
    {
        void Queue(EmailMessage message);
    }
}
```

EmailMessage.cs (created in Plan 05-03):
```csharp
public record EmailMessage(
    List<string> Recipients,
    string Subject,
    string Body,
    bool Bcc = false
);
```

Current NotificacionBusiness constructor (Notificacion.cs line 17):
```csharp
public NotificacionBusiness(IUtilidades utilidades, IDataContextFactory factory, ProveedorBusiness proveedor = null)
```

Target NotificacionBusiness constructor (after this plan):
```csharp
public NotificacionBusiness(IUtilidades utilidades, IDataContextFactory factory,
    IEmailQueue emailQueue, ProveedorBusiness proveedor = null)
```

Current PreguntasBusiness constructor (Preguntas.cs lines 13–20):
```csharp
public PreguntasBusiness(IUtilidades utilidades, IDataContextFactory factory)
{
    _utilidades = utilidades;
    _factory = factory;
}
```

Target PreguntasBusiness constructor (after this plan):
```csharp
public PreguntasBusiness(IUtilidades utilidades, IDataContextFactory factory, INotificacion notificacion)
{
    _utilidades = utilidades;
    _factory = factory;
    _notificacion = notificacion;
}
```

BusinessInstaller.cs line 22 (already registered):
```csharp
services.AddScoped<INotificacion, NotificacionBusiness>();
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace _utilidades.SendMail with _emailQueue.Queue in NotificacionBusiness</name>
  <files>
    Negocio.Business/Notificacion/Notificacion.cs
  </files>
  <read_first>
    - Negocio.Business/Notificacion/Notificacion.cs — read the FULL file to find: (a) constructor location, (b) every _utilidades.SendMail call (estimated 12+ in the switch block of GenerarNotificacion), (c) the exact method signatures (list of recipients, subject, message, bcc flag) for each call
    - Negocio.Business/Notificacion/INotificacion.cs — read full file to confirm the interface doesn't also declare SendMail
  </read_first>
  <action>
Step 1 — Add IEmailQueue field and update constructor in Notificacion.cs:

Add private field:
    private readonly IEmailQueue _emailQueue;

Update constructor signature from:
    public NotificacionBusiness(IUtilidades utilidades, IDataContextFactory factory, ProveedorBusiness proveedor = null)
To:
    public NotificacionBusiness(IUtilidades utilidades, IDataContextFactory factory, IEmailQueue emailQueue, ProveedorBusiness proveedor = null)

Add assignment in constructor body:
    _emailQueue = emailQueue;

Step 2 — Add using directive for Negocio.Business.Email namespace:
Add at the top of Notificacion.cs:
    using Negocio.Business.Email;

Step 3 — Replace every _utilidades.SendMail(...) call with _emailQueue.Queue(new EmailMessage(...)):

For each occurrence of:
    _utilidades.SendMail(correos, noti.Asunto, mensaje);
Replace with:
    _emailQueue.Queue(new EmailMessage(correos, noti.Asunto, mensaje));

For each occurrence of:
    _utilidades.SendMail(correos, noti.Asunto, mensaje, true);   // bcc = true
Replace with:
    _emailQueue.Queue(new EmailMessage(correos, noti.Asunto, mensaje, Bcc: true));

For each occurrence of:
    _utilidades.SendMail(correos, noti.Asunto, mensaje, false);  // bcc = false  
Replace with:
    _emailQueue.Queue(new EmailMessage(correos, noti.Asunto, mensaje));

Read the FULL Notificacion.cs to find the exact parameter patterns at each call site. The variable names for recipients (correos), subject (noti.Asunto or similar), and message (mensaje) may vary. Map each old call to its exact EmailMessage constructor equivalent.

Step 4 — Verify no _utilidades.SendMail references remain:
Run: grep "SendMail" Negocio.Business/Notificacion/Notificacion.cs
Expected: zero results.

Step 5 — Remove _utilidades from constructor if it is now only used for SendMail:
IMPORTANT: Before removing _utilidades from the constructor, grep all usages of _utilidades in Notificacion.cs. If _utilidades is still called for other methods (GetConstante, ObtenerBlob, etc.), keep it. Only remove if SendMail was the ONLY usage.
    grep "_utilidades\." Negocio.Business/Notificacion/Notificacion.cs
If zero results after removing SendMail calls, remove _utilidades from the constructor and delete the private field. Otherwise, keep it.
  </action>
  <verify>
    <automated>
      grep "_utilidades.SendMail" Negocio.Business/Notificacion/Notificacion.cs || echo "PASS: no SendMail calls remain"
      grep "_emailQueue.Queue" Negocio.Business/Notificacion/Notificacion.cs | wc -l | xargs echo "Queue calls count:"
      grep "IEmailQueue emailQueue" Negocio.Business/Notificacion/Notificacion.cs && echo "PASS: IEmailQueue in constructor"
      grep "using Negocio.Business.Email" Negocio.Business/Notificacion/Notificacion.cs && echo "PASS: namespace imported"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "_utilidades.SendMail" Negocio.Business/Notificacion/Notificacion.cs` returns no matches
    - `grep "_emailQueue.Queue" Negocio.Business/Notificacion/Notificacion.cs` returns at least 1 match
    - `grep "IEmailQueue" Negocio.Business/Notificacion/Notificacion.cs` returns a match in the constructor signature
    - `grep "using Negocio.Business.Email" Negocio.Business/Notificacion/Notificacion.cs` returns a match
  </acceptance_criteria>
  <done>NotificacionBusiness uses _emailQueue.Queue for all email dispatch. SendMail no longer referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PreguntasBusiness to inject INotificacion + verify build passes</name>
  <files>
    Negocio.Business/Preguntas/Preguntas.cs
  </files>
  <read_first>
    - Negocio.Business/Preguntas/Preguntas.cs — read full file to locate: (a) constructor definition, (b) all usages of "new NotificacionBusiness", (c) all call sites of GenerarNotificacion to understand the reference pattern, (d) whether _notificacion field already exists
  </read_first>
  <action>
Step 1 — Add INotificacion field to PreguntasBusiness if not present:
    private readonly INotificacion _notificacion;

Step 2 — Update constructor to inject INotificacion:
From:
    public PreguntasBusiness(IUtilidades utilidades, IDataContextFactory factory)
    {
        _utilidades = utilidades;
        _factory = factory;
    }
To:
    public PreguntasBusiness(IUtilidades utilidades, IDataContextFactory factory, INotificacion notificacion)
    {
        _utilidades = utilidades;
        _factory = factory;
        _notificacion = notificacion;
    }

Step 3 — Replace all inline new NotificacionBusiness(...) instantiations:
Find every occurrence of:
    (new NotificacionBusiness(_utilidades, _factory)).GenerarNotificacion(...)
Or any variation with new NotificacionBusiness.

Replace with:
    _notificacion.GenerarNotificacion(...)

The Thread wrapper is still present at this point — the Thread removal happens in Plan 05-05. This task ONLY replaces the `new NotificacionBusiness` with `_notificacion`. The `new Thread(() => ...)` wrapper stays for now.

Example transformation for lines ~79–85:
Before:
    Thread t = new Thread(() =>
        (new NotificacionBusiness(_utilidades, _factory)).GenerarNotificacion("referencia", obj)
    );
    t.Start();
After:
    Thread t = new Thread(() =>
        _notificacion.GenerarNotificacion("referencia", obj)
    );
    t.Start();

Step 4 — Add using for INotificacion namespace if needed:
Check if INotificacion is in Negocio.Business namespace. If so, no new using needed. If in a sub-namespace, add it.

Step 5 — Verify build succeeds:
Run: dotnet build PortalNegocioWS.sln
Expected: Build succeeded, 0 errors. After Plans 05-01, 05-03, 05-04, there should be no compile errors.

If build fails:
- NotificacionBusiness constructor call sites in other files (if any) may need updating to pass the IEmailQueue parameter. Since NotificacionBusiness is registered in DI (AddScoped), only files that do `new NotificacionBusiness(...)` manually need updating. Grep for "new NotificacionBusiness" to find them.
- Fix each found occurrence by adding the IEmailQueue argument from sp.GetRequiredService<IEmailQueue>() or by ensuring DI injection is used instead of manual instantiation.
  </action>
  <verify>
    <automated>
      grep "new NotificacionBusiness" Negocio.Business/Preguntas/Preguntas.cs || echo "PASS: no inline NotificacionBusiness instantiation"
      grep "INotificacion" Negocio.Business/Preguntas/Preguntas.cs && echo "PASS: INotificacion in constructor"
      grep "_notificacion.GenerarNotificacion" Negocio.Business/Preguntas/Preguntas.cs | wc -l | xargs echo "Notificacion call count:"
      dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded" && echo "PASS: zero errors"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "new NotificacionBusiness" Negocio.Business/Preguntas/Preguntas.cs` returns no matches
    - `grep "INotificacion" Negocio.Business/Preguntas/Preguntas.cs` returns at least one match in the constructor
    - `dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded"` is present in output
    - `dotnet build PortalNegocioWS.sln 2>&1 | grep " error "` returns no matches
  </acceptance_criteria>
  <done>PreguntasBusiness injects INotificacion. Build succeeds with zero errors. BGD-02 is partially complete (NotificacionBusiness now queues instead of SendMail; Thread wrappers still present — removed in Plan 05-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NotificacionBusiness → IEmailQueue.Queue | Email data (recipients, subject, body) crosses from business logic to queue; no new external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | EmailMessage recipients list | accept | Recipients are constructed from DB lookups (ObtenerCorreosProveedor etc), not from user input. Cannot be externally manipulated beyond what was possible before. |
| T-05-04-02 | Elevation of Privilege | ProveedorBusiness optional constructor arg | accept | The optional ProveedorBusiness arg in NotificacionBusiness constructor is unchanged. No new privilege path introduced. |
</threat_model>

<verification>
After both tasks:
1. `grep "_utilidades.SendMail" Negocio.Business/Notificacion/Notificacion.cs` — no output
2. `grep "new NotificacionBusiness" Negocio.Business/Preguntas/Preguntas.cs` — no output
3. `dotnet build PortalNegocioWS.sln 2>&1 | grep "Build succeeded"` — present
4. `grep "_emailQueue.Queue" Negocio.Business/Notificacion/Notificacion.cs | wc -l` — 1 or more
</verification>

<success_criteria>
- NotificacionBusiness uses _emailQueue.Queue instead of _utilidades.SendMail at all call sites
- PreguntasBusiness injects INotificacion and does not use new NotificacionBusiness inline
- dotnet build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-hygiene-background-jobs/05-04-SUMMARY.md`
</output>
