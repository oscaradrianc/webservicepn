---
phase: 04-api-contract-standardization
plan: 07
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - PortalNegocioWS/Controllers/CatalogoController.cs
  - PortalNegocioWS/Controllers/ConsultaController.cs
  - PortalNegocioWS/Controllers/UsuarioController.cs
  - PortalNegocioWS/Controllers/UtilidadesController.cs
  - PortalNegocioWS/Controllers/OpcionController.cs
  - PortalNegocioWS/Controllers/RolController.cs
  - PortalNegocioWS/Controllers/FormatoController.cs
  - PortalNegocioWS/Controllers/AutorizadorGerenciaController.cs
  - PortalNegocioWS/Controllers/ParametroGeneralController.cs
  - PortalNegocioWS/Controllers/ConstanteController.cs
  - PortalNegocioWS/Controllers/NoticiasController.cs
  - "PortalNegocioWS/Controllers/NotificacionController .cs"
  - PortalNegocioWS/Controllers/NotificacionUsuarioController.cs
  - PortalNegocioWS/Controllers/OpcionesRolController.cs
  - .planning/ANGULAR-MIGRATION.md
autonomous: true
requirements:
  - API-06
  - API-08
  - API-09

must_haves:
  truths:
    - "All 19 controllers inherit ApiControllerBase (zero remaining ': ControllerBase' in Controllers/)"
    - "All 19 controllers have [ApiController] — either inherited from ApiControllerBase or explicitly declared (both are valid)"
    - "ANGULAR-MIGRATION.md is complete — no '*Filled by Plan*' placeholder lines remain"
    - "The guide documents that GET-only controllers with no error paths require no Angular changes"
  artifacts:
    - path: "PortalNegocioWS/Controllers/CatalogoController.cs"
      provides: "CatalogoController inheriting ApiControllerBase"
      contains: "ApiControllerBase"
    - path: ".planning/ANGULAR-MIGRATION.md"
      provides: "Complete Angular migration guide"
      contains: "no placeholder lines"
  key_links:
    - from: "All 14 remaining controllers"
      to: "ApiControllerBase"
      via: ": ApiControllerBase"
      pattern: "class.*ApiControllerBase"
---

<objective>
Migrate the remaining 14 controllers (those not already migrated in Plans 03-05) to inherit ApiControllerBase. Verify that all 19 controllers have [ApiController] (API-08 — already satisfied but needs explicit confirmation). Complete ANGULAR-MIGRATION.md with the remaining controllers section.

Purpose: Per API-06 and API-08. The 14 remaining controllers (CatalogoController, ConsultaController, UsuarioController, UtilidadesController, OpcionController, RolController, FormatoController, AutorizadorGerenciaController, ParametroGeneralController, ConstanteController, NoticiasController, NotificacionController, NotificacionUsuarioController, OpcionesRolController) mostly have correct HTTP patterns already — they just need to inherit ApiControllerBase. Per API-09, the ANGULAR-MIGRATION.md document must be finalized.

Output: 14 controllers updated to inherit ApiControllerBase, ANGULAR-MIGRATION.md finalized.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-api-contract-standardization/04-CONTEXT.md
@.planning/phases/04-api-contract-standardization/04-PATTERNS.md
@.planning/phases/04-api-contract-standardization/04-04-SUMMARY.md
@.planning/phases/04-api-contract-standardization/04-05-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From ApiControllerBase (Plan 02 output):
```csharp
namespace PortalNegocioWS.Controllers
{
    [ApiController]
    public abstract class ApiControllerBase : ControllerBase
    {
    }
}
```

Two-namespace situation:
- Namespace SWNegocio.Controllers: CatalogoController, ConsultaController, UsuarioController,
  UtilidadesController, OpcionController, RolController, FormatoController
- Namespace PortalNegocioWS.Controllers: AutorizadorGerenciaController, ParametroGeneralController,
  ConstanteController, NoticiasController, NotificacionController, NotificacionUsuarioController,
  OpcionesRolController

For SWNegocio.Controllers controllers: add `using PortalNegocioWS.Controllers;` and change
`: ControllerBase` to `: ApiControllerBase`. Do NOT change the class namespace.

For PortalNegocioWS.Controllers controllers: same namespace as ApiControllerBase — just change
`: ControllerBase` to `: ApiControllerBase`, no additional using needed.

Migration change per controller (minimal):
1. Add `using PortalNegocioWS.Controllers;` (only for SWNegocio.Controllers controllers)
2. Change `: ControllerBase` → `: ApiControllerBase`
3. Keep all existing [ApiController] attributes (safe to keep — no double-apply issue)
4. NO other changes — do NOT modify any business logic, error handling, response patterns, or routes

Before migrating each controller, READ the full file first to check for any existing error patterns
that should also be fixed (any remaining Content(HttpStatusCode...) or StatusCode(500, ...) calls
that were missed in Plans 04 and 05). If found, fix them in the same edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 7 SWNegocio.Controllers controllers to ApiControllerBase</name>
  <files>
    PortalNegocioWS/Controllers/CatalogoController.cs,
    PortalNegocioWS/Controllers/ConsultaController.cs,
    PortalNegocioWS/Controllers/UsuarioController.cs,
    PortalNegocioWS/Controllers/UtilidadesController.cs,
    PortalNegocioWS/Controllers/OpcionController.cs,
    PortalNegocioWS/Controllers/RolController.cs,
    PortalNegocioWS/Controllers/FormatoController.cs
  </files>
  <read_first>
    - PortalNegocioWS/Controllers/CatalogoController.cs (read full content)
    - PortalNegocioWS/Controllers/ConsultaController.cs (read full content)
    - PortalNegocioWS/Controllers/UsuarioController.cs (read full content)
    - PortalNegocioWS/Controllers/UtilidadesController.cs (read full content)
    - PortalNegocioWS/Controllers/OpcionController.cs (read full content)
    - PortalNegocioWS/Controllers/RolController.cs (read full content)
    - PortalNegocioWS/Controllers/FormatoController.cs (read full content)
  </read_first>
  <action>
For each of the 7 controllers in namespace SWNegocio.Controllers, apply these changes:

1. Add `using PortalNegocioWS.Controllers;` to the using statements (at the top with other usings)
2. Change `: ControllerBase` to `: ApiControllerBase` in the class declaration
3. Keep all existing [ApiController] attributes
4. While reading each file: if any `Content(HttpStatusCode...)` or `StatusCode(500, ...)` patterns are found (missed in previous plans), fix them with the same patterns:
   - `Content(HttpStatusCode.BadRequest.ToString(), result)` → `throw new BusinessException(result)` (add `using PortalNegocioWS.Exceptions;` if needed)
   - `Content(HttpStatusCode.InternalServerError.ToString(), ex.Message)` → `throw;`
   - `StatusCode(500, e.Message)` in catch → remove try/catch, let exception propagate

Apply changes to all 7 files. After all 7 are modified, run `dotnet build` to verify.

Key expected changes per controller (based on research analysis):
- CatalogoController: already uses NotFound()/Ok() correctly — only base class change needed
- ConsultaController: GET-only endpoints, returns Response<T> directly — only base class change
- UsuarioController: mixed patterns, some IActionResult already — verify for any Content() bugs
- UtilidadesController: review for any error patterns
- OpcionController: review for any Content() calls (has some commented-out ResponseStatus direct returns)
- RolController: minimal endpoints — likely only base class change
- FormatoController: review for any error patterns
  </action>
  <verify>
    <automated>dotnet build D:/proyectos/PortalNegociosGithub/webservicepn/PortalNegocioWS.sln</automated>
  </verify>
  <acceptance_criteria>
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/CatalogoController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/ConsultaController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/UsuarioController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/UtilidadesController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/OpcionController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/RolController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/FormatoController.cs returns a match
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>7 SWNegocio.Controllers controllers inherit ApiControllerBase, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 7 PortalNegocioWS.Controllers controllers to ApiControllerBase</name>
  <files>
    PortalNegocioWS/Controllers/AutorizadorGerenciaController.cs,
    PortalNegocioWS/Controllers/ParametroGeneralController.cs,
    PortalNegocioWS/Controllers/ConstanteController.cs,
    PortalNegocioWS/Controllers/NoticiasController.cs,
    "PortalNegocioWS/Controllers/NotificacionController .cs",
    PortalNegocioWS/Controllers/NotificacionUsuarioController.cs,
    PortalNegocioWS/Controllers/OpcionesRolController.cs
  </files>
  <read_first>
    - PortalNegocioWS/Controllers/AutorizadorGerenciaController.cs (read full content)
    - PortalNegocioWS/Controllers/ParametroGeneralController.cs (read full content)
    - PortalNegocioWS/Controllers/ConstanteController.cs (read full content)
    - PortalNegocioWS/Controllers/NoticiasController.cs (read full content)
    - "PortalNegocioWS/Controllers/NotificacionController .cs" (read full content — note the space in filename)
    - PortalNegocioWS/Controllers/NotificacionUsuarioController.cs (read full content)
    - PortalNegocioWS/Controllers/OpcionesRolController.cs (read full content)
  </read_first>
  <action>
For each of the 7 controllers in namespace PortalNegocioWS.Controllers, apply these changes:

1. Change `: ControllerBase` to `: ApiControllerBase` in the class declaration
2. NO additional using needed (same namespace PortalNegocioWS.Controllers)
3. Keep all existing [ApiController] attributes
4. While reading each file: if any `Content(HttpStatusCode...)` or `StatusCode(500, ...)` patterns are found (missed in previous plans), fix them:
   - Add `using PortalNegocioWS.Exceptions;` if needed
   - Apply same fix patterns as Task 1

NOTE: "NotificacionController .cs" has a space in the filename. When editing this file, use the exact filename with the space. The controller class name inside is likely `NotificacionController`.

Apply changes to all 7 files. After all 7 are modified, run `dotnet build` to verify.

Final verification after this task: count all controllers inheriting ApiControllerBase — should be 19 total (5 migrated in Plans 03-05 + 7 in Task 1 + 7 in this task = 19).
  </action>
  <verify>
    <automated>dotnet build D:/proyectos/PortalNegociosGithub/webservicepn/PortalNegocioWS.sln</automated>
  </verify>
  <acceptance_criteria>
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/AutorizadorGerenciaController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/ParametroGeneralController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/ConstanteController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/NoticiasController.cs returns a match
    - grep "ApiControllerBase" "PortalNegocioWS/Controllers/NotificacionController .cs" returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/NotificacionUsuarioController.cs returns a match
    - grep "ApiControllerBase" PortalNegocioWS/Controllers/OpcionesRolController.cs returns a match
    - grep -rL "ApiControllerBase" PortalNegocioWS/Controllers/ | grep -v "ApiControllerBase.cs" returns empty (all controllers migrated)
    - grep -c ": ControllerBase" PortalNegocioWS/Controllers/*.cs returns 0 (no controller still inherits ControllerBase directly)
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>All 19 controllers inherit ApiControllerBase, zero ControllerBase direct inheritance remains in Controllers/, build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Complete ANGULAR-MIGRATION.md — remaining controllers section and final summary</name>
  <files>
    .planning/ANGULAR-MIGRATION.md
  </files>
  <read_first>
    - .planning/ANGULAR-MIGRATION.md (read full current state — all previous plan additions)
  </read_first>
  <action>
Replace the "Remaining Controllers (ApiControllerBase migration)" placeholder section and finalize the document.

The remaining 14 controllers (CatalogoController, ConsultaController, UsuarioController,
UtilidadesController, OpcionController, RolController, FormatoController,
AutorizadorGerenciaController, ParametroGeneralController, ConstanteController,
NoticiasController, NotificacionController, NotificacionUsuarioController, OpcionesRolController)
were all migrated to inherit ApiControllerBase. Their error paths were already using correct
IActionResult patterns (NotFound(), Ok(), BadRequest()) before Phase 4.

For these controllers, document:
1. They were migrated to ApiControllerBase (infrastructure change only)
2. Their existing error paths were already correct (HTTP 404, HTTP 400 via NotFound()/BadRequest())
3. **No Angular changes required** for these controllers — their HTTP status codes did not change
4. The only new behavior: if an UNHANDLED exception occurs (previously would have been a raw 500 plain text or swallowed), it now returns HTTP 500 ProblemDetails JSON. Angular global error interceptors should handle HTTP 500 uniformly per the Infrastructure section.

Add a final "Summary" section to the document:

```markdown
## Summary

### Controllers with breaking error contract changes (Angular update required)
| Controller | Endpoints Changed | HTTP Code Change |
|------------|-------------------|-----------------|
| LoginController | POST /authenticate | 200 → 401 for bad credentials |
| LoginController | POST /changepassword, /resetpassword | 200 → 400 for business errors |
| SolicitudController | POST /registrar, /actualizar, /Autorizar, /actualizarfechas | 200 → 400 for business errors |
| SolicitudController | POST /cargamasiva | 200 → 500 for unhandled exceptions |
| ProveedorController | POST /registrar, /autorizar, /actualizarestado, /actualizardocs | 200 → 400/500 |
| ProveedorController | GET /proveedorporestado, /proveedorpormes | 200 → 500 for unhandled exceptions |
| CotizacionController | POST /adjudicar | 500 → 400 (business error reclassified) |
| CotizacionController | POST /cargamasiva, GET /listarfichatecnica | 200 → 500 for exceptions |
| PreguntasController | POST /preguntar, /responder | 200 → 400 for business errors |

### Controllers with no Angular changes needed
All other controllers (CatalogoController, ConsultaController, UsuarioController, etc.) had correct HTTP status codes before Phase 4. No Angular client code changes required.

### Global change (all endpoints)
Unhandled server exceptions now return HTTP 500 ProblemDetails JSON. Angular's global HTTP interceptor should be updated to handle `error.status === 500` with `error.error?.detail` for user-facing messages.

*Document completed: Phase 4, Plan 07*
```

After adding the summary, verify there are NO remaining "*Filled by Plan*" placeholder lines in the document.
  </action>
  <verify>
    <automated>grep -c "Filled by Plan" .planning/ANGULAR-MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Filled by Plan" .planning/ANGULAR-MIGRATION.md returns 0 (no placeholders remain)
    - grep "Summary" .planning/ANGULAR-MIGRATION.md returns a match
    - grep "Controllers with breaking error contract changes" .planning/ANGULAR-MIGRATION.md returns a match
    - grep "Controllers with no Angular changes needed" .planning/ANGULAR-MIGRATION.md returns a match
    - grep "Document completed: Phase 4" .planning/ANGULAR-MIGRATION.md returns a match
  </acceptance_criteria>
  <done>ANGULAR-MIGRATION.md is complete with no placeholder lines, summary table present, per D-11/D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All 19 controllers → GlobalExceptionHandler | With base class inheritance complete, all controllers benefit from centralized error handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Information Disclosure | UsuarioController — verify no StatusCode(500, message) patterns remain | mitigate | Task 1 explicitly reads UsuarioController full content before editing. Any StatusCode(500, e.Message) found is fixed with `throw;`. GlobalExceptionHandler returns generic message for 500. |
| T-04-19 | Elevation of Privilege | NotificacionController — space in filename | accept | The file "NotificacionController .cs" with a space is a filesystem oddity. The controller class and routes are unaffected by the space in the .cs filename. ASP.NET Core routing uses the class name, not the filename. |
| T-04-20 | Information Disclosure | UsuarioController.CambiarClaveUsuario — returns ResponseStatus directly | accept | UsuarioController.CambiarClaveUsuario was noted in RESEARCH.md as returning ResponseStatus directly (no IActionResult). Per CONTEXT.md D-07/D-08, only LoginController migration is in scope for this phase. UsuarioController is out of scope. It still returns HTTP 200 for all responses. This is acceptable for Phase 4 scope. |
</threat_model>

<verification>
Build verification:
```bash
dotnet build D:/proyectos/PortalNegociosGithub/webservicepn/PortalNegocioWS.sln
```

Complete migration verification (all 19 controllers):
```bash
# Count controllers inheriting ApiControllerBase — should be 19 (excluding ApiControllerBase.cs itself)
grep -rl "ApiControllerBase" PortalNegocioWS/Controllers/ | grep -v "ApiControllerBase.cs" | wc -l
# Expected: 19

# Verify no controller still inherits ControllerBase directly
grep -r ": ControllerBase" PortalNegocioWS/Controllers/ | grep -v "ApiControllerBase.cs"
# Expected: empty output
```

Angular guide completeness:
```bash
grep -c "Filled by Plan" .planning/ANGULAR-MIGRATION.md
# Expected: 0
```
</verification>

<success_criteria>
- `dotnet build` passes with 0 errors
- All 19 controllers inherit ApiControllerBase (zero `: ControllerBase` in Controllers/ except ApiControllerBase.cs itself)
- All 19 controllers have [ApiController] — either inherited or explicit
- ANGULAR-MIGRATION.md is complete with zero placeholder lines, summary table, and clear distinction between controllers requiring Angular changes vs. those that do not
- Phase 4 goal fully satisfied: HTTP codes corrected, GlobalExceptionHandler active, DataAnnotations added, Angular migration guide complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-contract-standardization/04-07-SUMMARY.md`
</output>
