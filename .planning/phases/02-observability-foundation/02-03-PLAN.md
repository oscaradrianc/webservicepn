---
phase: 02-observability-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - PortalNegocioWS/StartupCopia.cs
  - PortalNegocioWS/PortalNegocioWS.csproj
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "StartupCopia.cs no existe en el repositorio"
    - "La entrada <Compile Remove=\"StartupCopia.cs\" /> está eliminada de PortalNegocioWS.csproj"
    - "La aplicación compila sin el archivo StartupCopia.cs"
    - "Ninguna referencia a la clase Startup de StartupCopia permanece en archivos compilados"
  artifacts:
    - path: "PortalNegocioWS/PortalNegocioWS.csproj"
      provides: "csproj sin la entrada Compile Remove de StartupCopia.cs"
      contains: "PackageReference"
  key_links:
    - from: "git rm PortalNegocioWS/StartupCopia.cs"
      to: "Repositorio limpio"
      via: "Eliminación física del archivo del árbol de trabajo y del índice git"
      pattern: "StartupCopia"
---

<objective>
Eliminar StartupCopia.cs del repositorio. El archivo es un remanente del patrón antiguo Startup/IWebHostBuilder y ya está excluido de la compilación (`<Compile Remove="StartupCopia.cs" />`). La funcionalidad relevante (UseSerilogRequestLogging) ya fue migrada a Program.cs en el Plan 02-01. Limpieza del csproj incluida.

Purpose: StartupCopia.cs es deuda técnica activa — confunde a futuros desarrolladores sobre cuál es el punto de entrada real. Eliminarlo cierra el Pre-work obligatorio definido en el ROADMAP para Phase 2.

Output: StartupCopia.cs eliminado del disco y del índice git. PortalNegocioWS.csproj sin la entrada Compile Remove. Compilación limpia.
</objective>

<execution_context>
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/workflows/execute-plan.md
@D:/proyectos/PortalNegociosGithub/webservicepn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-observability-foundation/02-CONTEXT.md
@.planning/phases/02-observability-foundation/02-01-SUMMARY.md

<interfaces>
<!-- Contexto del archivo a eliminar -->

StartupCopia.cs — clase Startup (legacy, excluida de compilación):
```csharp
// Namespace: PortalNegocioWS
// Clase: Startup (NO es el punto de entrada — Program.cs es el punto de entrada)
// Método relevante ya migrado: app.UseSerilogRequestLogging() → ya en Program.cs (Plan 02-01)
// Método NO relevante para migrar: UseDeveloperExceptionPage, UseHttpsRedirection, UseResponseCompression
//   → estos están excluidos de la configuración actual de Program.cs por diseño
// Línea 78: app.UseSerilogRequestLogging() — YA migrado a Program.cs en Plan 02-01
```

PortalNegocioWS.csproj — entrada a eliminar:
```xml
<ItemGroup>
  <Compile Remove="StartupCopia.cs" />   ← ELIMINAR esta línea
</ItemGroup>
```

(Si el ItemGroup queda vacío después de eliminar esa línea, eliminar el ItemGroup completo también)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar que UseSerilogRequestLogging está en Program.cs, luego eliminar StartupCopia.cs y limpiar csproj</name>
  <files>PortalNegocioWS/StartupCopia.cs, PortalNegocioWS/PortalNegocioWS.csproj</files>
  <read_first>
    - PortalNegocioWS/StartupCopia.cs — leer COMPLETO para confirmar que no contiene nada que no haya sido migrado a Program.cs. Buscar específicamente: (1) UseSerilogRequestLogging — debe estar en Program.cs, (2) cualquier installer, servicio o middleware que NO esté en Program.cs actual.
    - PortalNegocioWS/Program.cs — confirmar que app.UseSerilogRequestLogging() está presente (lo agregó Plan 02-01)
    - PortalNegocioWS/PortalNegocioWS.csproj — leer para ver la entrada Compile Remove exacta
  </read_first>
  <action>
PRE-CHECK OBLIGATORIO antes de eliminar:
- Verificar que `grep "UseSerilogRequestLogging" PortalNegocioWS/Program.cs` devuelve 1 resultado
- Si NO está en Program.cs, DETENER y reportar: "UseSerilogRequestLogging no encontrado en Program.cs — Plan 02-01 no completó correctamente. No eliminar StartupCopia.cs hasta resolver."

Si el pre-check pasa (UseSerilogRequestLogging está en Program.cs):

1. Eliminar el archivo con git rm (para que quede trackeado en el índice de git):
```bash
cd "D:/proyectos/PortalNegociosGithub/webservicepn"
git rm PortalNegocioWS/StartupCopia.cs
```

2. Abrir PortalNegocioWS/PortalNegocioWS.csproj y eliminar la siguiente línea:
```xml
<Compile Remove="StartupCopia.cs" />
```

Si el ItemGroup que contenía esa línea queda completamente vacío (sin otras entradas), eliminar también las líneas del ItemGroup:
```xml
<ItemGroup>
</ItemGroup>
```

(Solo eliminar el ItemGroup vacío — no tocar otros ItemGroups que tengan PackageReference, ProjectReference, Folder, etc.)

NO tocar ninguna otra sección del csproj.
  </action>
  <verify>
    <automated>cd "D:/proyectos/PortalNegociosGithub/webservicepn" && dotnet build PortalNegocioWS.sln 2>&1 | tail -5 && echo "---" && test -f PortalNegocioWS/StartupCopia.cs && echo "ERROR: archivo aun existe" || echo "OK: archivo eliminado"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build PortalNegocioWS.sln` exits 0
    - `test -f PortalNegocioWS/StartupCopia.cs` devuelve exit code 1 (archivo no existe)
    - `grep "StartupCopia" PortalNegocioWS/PortalNegocioWS.csproj` devuelve 0 resultados
    - `git status` muestra StartupCopia.cs como "deleted" (trackeado por git rm, no como untracked)
    - `grep -r "class Startup" PortalNegocioWS/ 2>/dev/null` devuelve 0 resultados en archivos compilados
  </acceptance_criteria>
  <done>StartupCopia.cs eliminado del disco y del índice git. csproj limpio sin la entrada Compile Remove. Aplicación compila sin el archivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eliminación de archivo | Operación destructiva irreversible sin backup — depende de git para recuperación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | git rm StartupCopia.cs | accept | Operación de eliminación es reversible vía `git checkout` si fue cometida. El pre-check obligatorio (confirmar que UseSerilogRequestLogging está en Program.cs) previene pérdida de configuración. |
| T-02-03-02 | Information Disclosure | StartupCopia.cs contenía UseAuthentication/UseAuthorization order | accept | La copia de Startup tiene auth middleware en orden distinto al de Program.cs (UseAuthorization antes que UseAuthentication). Program.cs usa el orden correcto para .NET 6+. No hay disclosure de datos — es un remanente de código legacy ya excluido de compilación. |
</threat_model>

<verification>
Después de completar la tarea:

1. `dotnet build PortalNegocioWS.sln` — debe compilar sin errores
2. `test -f PortalNegocioWS/StartupCopia.cs && echo EXISTS || echo DELETED` — debe mostrar DELETED
3. `grep "StartupCopia" PortalNegocioWS/PortalNegocioWS.csproj` — debe devolver 0 resultados
4. `git status` — debe mostrar StartupCopia.cs como eliminado (no como untracked)
</verification>

<success_criteria>
- StartupCopia.cs no existe en el filesystem
- PortalNegocioWS.csproj no tiene entrada `<Compile Remove="StartupCopia.cs" />`
- `dotnet build PortalNegocioWS.sln` exits 0
- El archivo está trackeado como eliminado en git (no simplemente ignorado)
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-foundation/02-03-SUMMARY.md` with:
- Confirmation of pre-check result (UseSerilogRequestLogging found in Program.cs before deletion)
- Confirmation that StartupCopia.cs was deleted
- Any content in StartupCopia.cs that was NOT in Program.cs (as documentation that nothing was lost)
- Build result (pass/fail)
</output>
